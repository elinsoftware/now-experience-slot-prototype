# Slots and Action handlers prototupe

`index.js` is a main file that declares/renders slots defines an action.

`now-ui.json` provides a component specification for UI Builder, that includes slots and actions.
